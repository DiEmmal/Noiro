services:
  mongo-db:
    image: mongo:7
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: noiro
      MONGO_INITDB_ROOT_PASSWORD: 123456
    volumes:
      - ./mongo/:/data/db
    ports:
      - 27017:27017